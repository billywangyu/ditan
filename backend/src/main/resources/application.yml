server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ditan
    username: postgres
    password: 
  jpa:
    database-platform: org.hibernate.spatial.dialect.postgis.PostgisDialect
    hibernate:
      ddl-auto: update
    show-sql: true
